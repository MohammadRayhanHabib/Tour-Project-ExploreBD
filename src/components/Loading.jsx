import React from 'react';

const Loading = () => {
    return (
        <div>
            <span className="loading loading-bars loading-xs"></span>

        </div>
    );
};

export default Loading;